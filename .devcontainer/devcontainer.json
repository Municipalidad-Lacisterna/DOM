{
  "name": "DOM en Línea Municipal",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "postCreateCommand": "docker compose up --build -d && sleep 8 && docker compose exec api python -m app.create_tables && docker compose exec api python -m app.seed",
  "forwardPorts": [5173, 8001],
  "portsAttributes": {
    "5173": { "label": "Portal web DOM", "onAutoForward": "openBrowser" },
    "8001": { "label": "API (Swagger /docs)", "onAutoForward": "silent" }
  },
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "dbaeumer.vscode-eslint"]
    }
  }
}